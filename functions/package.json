{
  "name": "wahajplayz-portfolio-functions",
  "private": true,
  "main": "lib/index.js",
  "type": "commonjs",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^10.3.0",
    "firebase-functions": "^4.9.0",
    "stripe": "^18.4.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
